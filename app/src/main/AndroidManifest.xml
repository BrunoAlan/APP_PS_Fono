<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".logica.Ejercicio_Completar_Frase_NO_Opciones"></activity>
        <activity android:name=".logica.Ejercicio_Cinco_Opciones" />
        <activity android:name=".AdminitrarSonidos" />
        <activity
            android:name=".AgregarSonido"
            android:theme="@style/AppTheme.PopUpGrabar" />
        <activity android:name=".Configuracion" />
        <activity android:name=".logica.Practica" />
        <activity android:name=".logica.Ejercicio_Completar" />
        <activity android:name=".ConfiguracionAgregarSonido" />
        <activity android:name=".logica.Ejercicio_Tres_Opciones" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>